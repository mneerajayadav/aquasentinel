# AquaSentinel — Environment Variables
# Person 4 owns this file.
# Copy to .env and fill in values. NEVER commit .env to git.

# Database (PostgreSQL + PostGIS)
DATABASE_URL=postgresql://aqua:aqua1234@localhost:5432/aquasentinel

# Redis Cache
REDIS_URL=redis://localhost:6379

# NASA Earthdata (free account at urs.earthdata.nasa.gov)
NASA_USERNAME=your_earthdata_username
NASA_PASSWORD=your_earthdata_password

# Frontend API URL (change to deployed backend URL for production)
VITE_API_URL=http://localhost:8000
